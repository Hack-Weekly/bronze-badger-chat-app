{  
  "name": "bronze-badger-chat-app-client",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@nuxt/eslint-config": "^0.1.1",
    "@nuxtjs/tailwindcss": "^6.6.0",
    "eslint": "^8.36.0",
    "nuxt": "^3.3.1"
  }
}
